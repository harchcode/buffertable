import { u8, u16, u32, i8, i16, i32, f32, f64, bool } from './bptype';

export const SCHEMA_SIZE_TYPE = u8;
export const SCHEMA_TYPE = u8;
export const STR_OFFSET_TYPE = u8;
export const STR_BUFFER_SIZE_TYPE = u32;
export const STR_SIZE_TYPE = u16;
export const DATA_SIZE_TYPE = u32;
